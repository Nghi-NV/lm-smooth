name: lm_smooth
description: "High-performance masonry grid for Flutter with precomputed layout, large-list scrolling, and long-press drag reorder."
version: 0.1.0
homepage: https://github.com/Nghi-NV/lm-smooth
repository: https://github.com/Nghi-NV/lm-smooth
issue_tracker: https://github.com/Nghi-NV/lm-smooth/issues
topics:
  - grid
  - masonry
  - staggered
  - performance
  - dragdrop

environment:
  sdk: ^3.11.1
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
